---
name: ui-reskin-collab
description: >-
  設計師在「既有專案」上做 UI 換皮 / 改視覺(換配色、字體、排版、主題、樣式、
  reskin / restyle / retheme)時的協作流程。核心:只動視覺層,不踩到工程師的程式
  邏輯(資料 / API / 型別 / 狀態 / business logic)。每個專案的程式結構、技術棧、
  工程師習慣都不同,所以這個 skill 一定先「探勘」該專案的視覺層 vs 邏輯層邊界,
  再開始改。Use this whenever a designer (or anyone doing visual-only work) starts
  changing the look of an existing codebase they don't own the logic of —
  reskinning, restyling, retheming, adjusting colors / fonts / spacing / layout,
  or collaborating with backend / RD engineers on UI — especially when they worry
  about breaking the engineers' code. Trigger even if they don't say "skill":
  phrases like 換皮, 改 UI, 改樣式, 改配色, 接手前端視覺, restyle, reskin,
  theme the app, "我只想改 UI 不要動到後端", "跟工程師 / RD 合作改前端".
---

# UI Reskin Collaboration — 設計師換皮不踩邏輯

你正在一個**別人(工程師 / RD)擁有程式邏輯**的既有專案上做視覺改動。你的目標是:把皮換漂亮,同時**完全不弄壞工程師的資料層 / 邏輯**,而且讓對方能輕鬆 review、信任你的改動。

最大的陷阱不是技術,是**邊界**。「哪些檔是視覺、哪些是邏輯」每個專案畫法都不一樣。所以這個 skill 的第一步永遠是**探勘這個專案的邊界**,而不是直接動手。憑上一個專案的假設動手,就是踩雷的開始。

---

## 核心心智模型

> **視覺層是你的,邏輯層是工程師的。** 但這條線每個 codebase 畫的位置不同 —— 先找出「這個專案」的線,再開工。

| 通常是你能動的「視覺層」 | 通常是工程師的「邏輯層」(別碰) |
| --- | --- |
| 樣式 / template / CSS / class | 資料抓取(fetch / API client / query) |
| design token / 主題變數 / 配色 | adapter / mapper(資料轉換) |
| 共用視覺元件、圖檔、字體 | 型別 / schema / 資料模型 |
| 排版 / RWD / 間距 / 動效 | 狀態管理 store、business logic |
| | WebSocket / 即時資料、欄位順序與計算公式 |

鐵則:**改 token 不改 class(改中央樣式源,不散改);改視覺不改 logic。**

---

## Step 0 — 進場先探勘邊界(最重要,別跳過)

每次新專案的程式結構不同,所以**先把這個專案的邊界搞清楚**,寫下來,再開始改。

1. **找現成的協作 / 換皮指南。** 讀專案根目錄與 docs:`README`、`CONTRIBUTING`、`CLAUDE.md`、`docs/theming*`、`docs/design*`、`STYLEGUIDE`。有些團隊已經寫好「UI team 只動這幾個檔」的指南 —— 有就直接照它走。

2. **定位視覺層(你的領地)。** 搜尋專案裡的中央樣式源,常見形態:
   - design token / 主題:`colors.*`、`theme.*`、`tokens.*`、`tailwind.config.*`、`:root { --… }`、SCSS `$變數`、styled-components `theme`、CSS variables、design-system 套件
   - 樣式與畫面:`*.css` / `*.scss`、元件的 template/style 區、`components/`、`styles/`、`assets/`(圖檔字體)
   - 問自己:**改一個地方就能讓全站皮膚變的「中央開關」在哪?** 那就是你的主戰場。

3. **定位邏輯層(地雷區)。** 找出這些,標記為「不碰」:資料抓取 / API client、adapter/mapper、型別/schema、狀態 store、WebSocket、商業規則、後端服務(`api/`、`server/`、`services/`、`store/`、`hooks` 中的資料 hook)。

4. **不確定就問,別猜。** 給工程師三個問題(複製即用):
   > 1. 我要做 UI 換皮,**哪些資料夾/檔是「視覺層」我可以動**?
   > 2. **design token / 主題色 / 樣式變數放在哪個檔**?
   > 3. **有哪些是「碰了會壞」的邏輯/資料層我絕對不要動**?(adapter / API / 型別 / store…)

5. **產出「邊界地圖」並寫下來。** 在 `discuss/` 或變更 log 開頭記一段:這個專案哪裡是視覺(我動)、哪裡是邏輯(不動)、token 在哪。之後每次改動都對照它。

> 為什麼重要:邊界一旦講清楚並白紙黑字寫下,工程師 review 你的 PR 時知道你不會亂碰資料,信任就建立了 —— 後面 80% 的衝突與來回都不會發生。

### 沒有現成協作文件時怎麼辦(很常見)

多數專案根本沒有換皮 / 協作指南。沒有時,把第 1 步的「找文件」換成「**自己從程式碼推斷邊界,再拿去跟工程師確認**」:

1. **從程式碼訊號推斷(別憑空猜)**:
   - **中央樣式源**:grep hex 色碼、CSS `--變數`、`theme` / `token` / `tailwind.config` / SCSS `$變數` → 顏色集中處就是視覺主場。
   - **資料夾結構**:`components/` `styles/` `assets/`(視覺) vs `api/` `services/` `store/` `types/` `adapters/`(邏輯)。
   - **import 方向**:檔案 import 資料 / API → 偏邏輯;import 樣式 / token → 偏視覺。
   - **技術棧線索**:讀 `package.json`(或對應依賴清單)。tailwind / styled-components / sass → 視覺在哪;axios / react-query / zustand 等 → 邏輯在哪。
   - **誰擁有**:有 `CODEOWNERS` 就讀它;沒有就 `git log` / `git shortlog -sn -- <dir>` 看各資料夾主要誰在改 —— 工程師常碰的目錄就是邏輯區。
2. **自己先建「邊界地圖草稿」**,把檔分三類:視覺(我動)/ 邏輯(不碰)/ **不確定**。
3. **拿草稿給工程師確認,而不是丟開放式問題。** 給他一份「我理解的邊界,對嗎?」比問「哪些我能動?」更省他時間,也顯示你有先做功課:
   > 「這專案沒換皮文件,我看了一輪,理解是:這些是視覺層我會改(…)、這些是邏輯層我不碰(…)。對嗎?有要補的嗎?」
4. **灰色地帶**(同一檔混了抓資料 + template):**只動 template / style 那段,不碰 script / 邏輯**;仍不確定就先當禁區。
5. **用小而可逆的改動探邊界**:改一個 token / 顏色,觀察影響範圍多大、是否只動到視覺 —— 安全的 canary,出事也好還原。
6. **把確認過的邊界地圖寫下來;既然這專案沒文件,順手補一份簡單的換皮 / 協作指南**(就是這個 skill 的產物)。下一個設計師和工程師都受惠,你也把「沒有文件」這個坑順手填了。

> 原則:**沒有文件 ≠ 可以亂動。** 缺文件時更要靠「推斷 + 確認 + 小步試探」三件套,寧可多問一句、先當禁區,也不要憑上一個專案的假設動手。

---

## Step 1 — 改 UI 的鐵律

1. **改中央樣式源,不散改 class。** 改一處 token / 主題變數,全站皮膚跟著變;去改各元件的 class 命名或硬寫值,會擴散到上千行、難收尾、也容易動到別人的東西。
2. **對齊「設計來源」,別憑感覺。** 顏色 / 排版 / 欄位呈現先對照設計依據(Figma / 原生 App / spec / 截圖)。把「美感爭論」變成「對不對得上設計來源」這種客觀問題,工程師就不會質疑你的決策。
3. **重用既有共用元件,別重造。** 探勘時記下專案已有的視覺元件(按鈕、圖示、骨架、空狀態…),沿用它們,改一次全站一致。
4. **不硬寫值。** 該進 token 的顏色/尺寸不要寫死在元件裡 —— 否則下次換皮又要動上千行,也讓 diff 變髒。

### 特例:專案沒有 token 架構(到處 hardcode)時 —— 先把 token 層建起來

有些專案沒有任何 token / 主題概念,顏色、字級、間距全散落硬寫在各元件裡,連「中央開關」都沒有。這時你得**先用現有的值反建一套 token 層**,才有得換皮。

> ⚠️ 先認清:**建 token 層是「結構重構」,會動到大量檔,已經超出「純視覺改動」。** 不能默默做。先跟工程師說明、取得同意、講好範圍 —— 否則大範圍 diff 會嚇到對方、也容易跟他的改動撞。範圍通常**只 tokenize「這次換皮會動到的畫面」**,不要一次掃全站(別 boil the ocean)。

流程:

1. **盤點現有值(audit)**:grep 整個 codebase 的 hex / `rgb(a)`、字級、間距、圓角、陰影,統計頻率。會發現一堆「其實同一個色的不同寫法」(`#fff` / `#ffffff` / `white`)。
2. **收斂成語意化調色盤(cluster)**:近似值合併;命名用語意而非編號 —— 不是 `color-1`,而是 `--color-bg` / `--color-accent` / `--color-up` / `--color-down`。有設計來源(Figma / 原生 App)就對齊它。
3. **建立中央 token 層**:放在該技術棧慣用處(CSS `:root` vars / Tailwind theme / SCSS map / JS theme object)。**第一版 token 值 = 現狀的值**,先別改外觀。
4. **遷移引用,且第一輪「視覺零變化」**:把散落的 hardcoded 值逐步換成 token 引用,這輪畫面要**完全不變**(只是換寫法)。如此 diff 純粹是重構,工程師好 review,也不跟視覺改動混在一起。可分批 PR(一區一批)。
5. **截圖比對驗證**:before == after,確認只是換引用、畫面沒變。
6. **這時才真正換皮**:有中央開關了,改 token 值就全站變 —— 回到上面 Step 1 的正常 reskin 流程。

⚠️ 灰色地帶:有些 hardcoded 顏色是**邏輯驅動**的(圖表庫 config、條件塗色函式),那些可能落在邏輯層 —— 搬走前先確認,別當成單純樣式處理。

> 為什麼分兩階段(先重構成 token、再改值):把「換寫法」和「換外觀」拆開,每個 PR 只做一件事,review 容易、出錯好定位,也守住「視覺改動可獨立驗證」的原則。

---

## Step 2 — 隔離分支 + 變更 log(給工程師 review 的交接物)

- **開隔離分支**(語意命名,如 `feat/ui-*` / `restyle-*`),基底是主線,**不要直接改 main**。
- **維護一份變更 log** 讓工程師 review。每筆固定格式:

  ```
  ### YYYY-MM-DD — <一句話說明>
  - 檔案:<path>
  - 改了什麼:…
  - 視覺意圖:…
  - 風險 / 注意:…（無就寫「無」）
  ```

- log 開頭寫明邊界規則(只動視覺、不動邏輯)。這是**信任機制** —— 工程師看一份就懂你動了什麼、為什麼、有沒有風險,不用逐行猜。
- 養成「每次 commit 同步補一行 log」的習慣,否則一忙就斷更,事後要從 git 硬重建。**commit message 也寫細**,它是 log 斷更時的安全網。

---

## Step 3 — 跟主線合併:讓衝突「大聲爆出來」

- **每天 pull 主線**(別累積)。工程師的改動小塊小塊進來,衝突就小而可控;拖久了會跟你的大改版正面對撞,合併變地獄。
- **衝突解法原則**:視覺相關(template / style / token)→ **以你為主**;資料 logic / adapter / 型別 / API → **保留工程師的**。
- 合併用 merge 讓衝突顯性化,不要用會默默重寫歷史、把已解衝突重新觸發的方式。
- ⚠️ 你以為的「純視覺」改動可能動到**共用元件的介面(props / 事件)**。改介面前,先確認誰在用,否則合併時對方那邊會接不上。

---

## Step 4 — 被邏輯層卡住:開「請求單」,別自己改後端

當你被資料 / API / 權限 / 邏輯擋住(例:資料拿不到、API 回錯、需要新欄位):**不要自己去改邏輯層**。寫一份結構化請求給工程師,放 `discuss/` 或開 ticket,內含:

- **要什麼**:一句話講清楚需求
- **在哪**:相關 route / 檔案 / 元件
- **怎麼重現**:步驟 + 觀察到的行為
- **參考**:設計來源(Figma / 原生 App / spec)對應位置
- **我方可配合**:你能幫忙測試 / 提供哪些資訊

> 為什麼:這是高品質的非同步交接,工程師不用追問就能接手;你也守住了「不踩邏輯」的界線。

---

## Step 5 — 驗證(沒過 = 沒做完)

- 跑專案的檢查再宣稱完成:typecheck / build / lint / 測試。指令通常在 `package.json` scripts、`Makefile`、或 CI 設定裡 —— 探勘時就找出來。
- **視覺一定要在瀏覽器實際操作驗**:關鍵畫面 + RWD 斷點。typecheck 過不代表畫面對。
- 改完**不要主動 deploy**,等專案負責人確認。

---

## 避雷清單(踩過的坑,別重蹈)

- **別養長命分支** → 每天 pull,把大改拆成數個短命分支。
- **一次只跑一個 dev server** → 多實例會搶 port,常導致登入 / 連線壞掉、白費除錯時間。
- **dev-only hack(假登入 / 解鎖預覽旗標)要標記 + push 前清掉** → 留在共用程式碼是隱患,尤其碰到登入 / 付費這種敏感區。
- **視覺微調設停損** → 呼吸燈 / 寬度這種細節容易無止境調最後還 revert。動手前先跟對方對齊「驗收標準長怎樣」。
- **別假裝會手寫變更 log** → 要嘛養成習慣,要嘛一開始就靠 commit message + 一支腳本自動產。

---

## 不要做的事(Anti-goals)

- 不動資料欄位的順序 / 名稱 / 計算公式(會跟設計來源 / 後端不一致)。
- 不為了視覺去改 API / 資料 logic / 型別。
- 不把顏色 / 尺寸硬寫在元件裡(一律走 token)。
- 不改 build 產物(`dist/` 等,會被覆寫)。
- 不在不確定邊界時硬猜 —— 回到 Step 0 問清楚。

---

## 一句話總結

**進場先探勘邊界 → 改 token 不改 class、對齊設計來源 → 隔離分支 + 寫 log → 每天 pull 讓衝突顯性化 → 被邏輯卡住開請求單,不自己改後端 → 驗證再交付。** 最大價值是邊界透明與交接清楚;最大風險是憑上一個專案的假設動手。
